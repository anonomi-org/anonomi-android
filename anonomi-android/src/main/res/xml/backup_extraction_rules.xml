<?xml version="1.0" encoding="utf-8"?>
<data-extraction-rules>
	<cloud-backup disableIfNoEncryptionCapabilities="true">
		<!-- docs: https://developer.android.com/guide/topics/data/autobackup#XMLSyntax -->
		<exclude
			domain="root"
			path="." />
		<!-- the previous entry should exclude things recursively, but we add more just to be sure -->
		<exclude
			domain="root"
			path="app_db" />
		<exclude
			domain="root"
			path="app_key" />
		<!-- we don't use domain=file (getFilesDir()), but ApplicationInfo#dataDir -->
		<exclude
			domain="sharedpref"
			path="." />
		<!-- domain=root is internal storage only, so it does not reach the map
		     tiles in getExternalFilesDir(), which are in the default backup set -->
		<exclude
			domain="external"
			path="." />
	</cloud-backup>
	<device-transfer>
		<!-- docs: https://developer.android.com/guide/topics/data/autobackup#XMLSyntax -->
		<exclude
			domain="root"
			path="." />
		<!-- the previous entry should exclude things recursively, but we add more just to be sure -->
		<exclude
			domain="root"
			path="app_db" />
		<exclude
			domain="root"
			path="app_key" />
		<!-- we don't use domain=file (getFilesDir()), but ApplicationInfo#dataDir -->
		<exclude
			domain="sharedpref"
			path="." />
		<!-- allowBackup=false does not reach device-to-device transfer on some
		     manufacturers' devices from API 31, so external has to be listed
		     here too or the map tiles ride along -->
		<exclude
			domain="external"
			path="." />
	</device-transfer>
</data-extraction-rules>
